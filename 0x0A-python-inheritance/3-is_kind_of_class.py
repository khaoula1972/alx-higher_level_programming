#!/usr/bin/python3
"""
A module that contains a function that tells if the object
"""


def is_kind_of_class(obj, a_class):
    """
    A function that returns True if the object is kind of an instance
    """
    return (isinstance(obj, a_class))
